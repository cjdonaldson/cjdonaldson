# git-ings
a set of scripts and git settings applicable globally
- pre-push hook - pre push checks and project tests
- rmtargets - removing all build target directories
- rmorig - remove all merge conflict files

### setup
this script is a one time setup adding hooks and scripts
```bash
> ./setup.sh
```
